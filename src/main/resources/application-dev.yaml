spring:
  datasource:
    url: jdbc:mysql://localhost:3306/team02_dev?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "${custom.dev.db.password}"
  data:
    redis:
      host: localhost
      port: 6379
      password: "${custom.dev.redis.password}"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
